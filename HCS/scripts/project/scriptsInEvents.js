


const scriptsInEvents = {

	async E_app_Event4_Act1(runtime, localVars)
	{
// Создаем новый элемент <style>
const style = document.createElement('style');
style.type = 'text/css';

// Добавляем стили в элемент <style>
style.innerHTML = `
    /* Стили для элемента с id number */
    #number {
        background-color: transparent;
        border: none;
        outline: none;
        text-align: center;
        padding: 10px;
        -moz-appearance: textfield;
        -webkit-appearance: none;
        font-family: 'SFProText-Medium', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        font-size: 18px;
        color: rgb(125, 125, 125);
    }
    #number::placeholder {
        font-size: 14px;
        color: rgb(140, 140, 140);
    }
    #number::-webkit-inner-spin-button,
    #number::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }

    /* Стили для элемента с id price */
    #price {
        background-color: transparent;
        border: none;
        outline: none;
        text-align: center;
        padding: 10px;
        -moz-appearance: textfield;
        -webkit-appearance: none;
        font-family: 'SFProText-Medium', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        font-size: 16px;
        color: rgb(125, 125, 125);
    }
    #price::placeholder {
        font-size: 13px;
        color: rgb(140, 140, 140);
    }
    #price::-webkit-inner-spin-button,
    #price::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }
`;

// Вставляем элемент <style> в <head> документа
document.head.appendChild(style);
	},

	async E_app_Event7_Act10(runtime, localVars)
	{
// Создаем новый элемент <style>
const style = document.createElement('style');
style.type = 'text/css';

// Добавляем стили в элемент <style>
style.innerHTML = `
    /* Стили для элемента с id number */
    #number {
        background-color: transparent;
        border: none;
        outline: none;
        text-align: center;
        padding: 10px;
        -moz-appearance: textfield;
        -webkit-appearance: none;
        font-family: 'SFProText-Medium', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        font-size: 18px;
        color: rgb(125, 125, 125);
    }
    #number::placeholder {
        font-size: 14px;
        color: rgb(140, 140, 140);
    }
    #number::-webkit-inner-spin-button,
    #number::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }

    /* Стили для элемента с id price */
    #price {
        background-color: transparent;
        border: none;
        outline: none;
        text-align: center;
        padding: 10px;
        -moz-appearance: textfield;
        -webkit-appearance: none;
        font-family: 'SFProText-Medium', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        font-size: 16px;
        color: rgb(125, 125, 125);
        filter: blur(4px); /* Эффект размытия */
    }
    #price::placeholder {
        font-size: 13px;
        color: rgb(140, 140, 140);
    }
    #price::-webkit-inner-spin-button,
    #price::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }
`;

// Вставляем элемент <style> в <head> документа
document.head.appendChild(style);
	},

	async E_app_Event60_Act2(runtime, localVars)
	{
// Создаем новый элемент <style>
const style = document.createElement('style');
style.type = 'text/css';

// Добавляем стили в элемент <style>
style.innerHTML = `
    /* Стили для элемента с id number */
    #number {
        background-color: transparent;
        border: none;
        outline: none;
        text-align: center;
        padding: 10px;
        -moz-appearance: textfield;
        -webkit-appearance: none;
        font-family: 'SFProText-Medium', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        font-size: 18px;
        color: rgb(125, 125, 125);
    }
    #number::placeholder {
        font-size: 14px;
        color: rgb(140, 140, 140);
    }
    #number::-webkit-inner-spin-button,
    #number::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }

    /* Стили для элемента с id price */
    #price {
        background-color: transparent;
        border: none;
        outline: none;
        text-align: center;
        padding: 10px;
        -moz-appearance: textfield;
        -webkit-appearance: none;
        font-family: 'SFProText-Medium', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        font-size: 16px;
        color: rgb(125, 125, 125);
        /* Убираем размытие */
        filter: none;
    }
    #price::placeholder {
        font-size: 13px;
        color: rgb(140, 140, 140);
    }
    #price::-webkit-inner-spin-button,
    #price::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }
`;

// Вставляем элемент <style> в <head> документа
document.head.appendChild(style);
	},

	async E_app_Event90_Act8(runtime, localVars)
	{
// Создаем новый элемент <style>
const style = document.createElement('style');
style.type = 'text/css';

// Добавляем стили в элемент <style>
style.innerHTML = `
    /* Стили для элемента с id number */
    #number {
        background-color: transparent;
        border: none;
        outline: none;
        text-align: center;
        padding: 10px;
        -moz-appearance: textfield;
        -webkit-appearance: none;
        font-family: 'SFProText-Medium', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        font-size: 18px;
        color: rgb(125, 125, 125);
    }
    #number::placeholder {
        font-size: 14px;
        color: rgb(140, 140, 140);
    }
    #number::-webkit-inner-spin-button,
    #number::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }

    /* Стили для элемента с id price */
    #price {
        background-color: transparent;
        border: none;
        outline: none;
        text-align: center;
        padding: 10px;
        -moz-appearance: textfield;
        -webkit-appearance: none;
        font-family: 'SFProText-Medium', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        font-size: 16px;
        color: rgb(125, 125, 125);
        filter: blur(4px); /* Эффект размытия */
    }
    #price::placeholder {
        font-size: 13px;
        color: rgb(140, 140, 140);
    }
    #price::-webkit-inner-spin-button,
    #price::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }
`;

// Вставляем элемент <style> в <head> документа
document.head.appendChild(style);
	},

	async E_app_Event92_Act8(runtime, localVars)
	{
// Создаем новый элемент <style>
const style = document.createElement('style');
style.type = 'text/css';

// Добавляем стили в элемент <style>
style.innerHTML = `
    /* Стили для элемента с id number */
    #number {
        background-color: transparent;
        border: none;
        outline: none;
        text-align: center;
        padding: 10px;
        -moz-appearance: textfield;
        -webkit-appearance: none;
        font-family: 'SFProText-Medium', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        font-size: 18px;
        color: rgb(125, 125, 125);
    }
    #number::placeholder {
        font-size: 14px;
        color: rgb(140, 140, 140);
    }
    #number::-webkit-inner-spin-button,
    #number::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }

    /* Стили для элемента с id price */
    #price {
        background-color: transparent;
        border: none;
        outline: none;
        text-align: center;
        padding: 10px;
        -moz-appearance: textfield;
        -webkit-appearance: none;
        font-family: 'SFProText-Medium', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        font-size: 16px;
        color: rgb(125, 125, 125);
        /* Убираем размытие */
        filter: none;
    }
    #price::placeholder {
        font-size: 13px;
        color: rgb(140, 140, 140);
    }
    #price::-webkit-inner-spin-button,
    #price::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }
`;

// Вставляем элемент <style> в <head> документа
document.head.appendChild(style);
	},

	async E_app_Event98_Act2(runtime, localVars)
	{
// Создаем новый элемент <style>
const style = document.createElement('style');
style.type = 'text/css';

// Добавляем стили в элемент <style>
style.innerHTML = `
    /* Стили для элемента с id number */
    #number {
        background-color: transparent;
        border: none;
        outline: none;
        text-align: center;
        padding: 10px;
        -moz-appearance: textfield;
        -webkit-appearance: none;
        font-family: 'SFProText-Medium', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        font-size: 18px;
        color: rgb(125, 125, 125);
    }
    #number::placeholder {
        font-size: 14px;
        color: rgb(140, 140, 140);
    }
    #number::-webkit-inner-spin-button,
    #number::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }

    /* Стили для элемента с id price */
    #price {
        background-color: transparent;
        border: none;
        outline: none;
        text-align: center;
        padding: 10px;
        -moz-appearance: textfield;
        -webkit-appearance: none;
        font-family: 'SFProText-Medium', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        font-size: 16px;
        color: rgb(125, 125, 125);
        filter: blur(4px); /* Эффект размытия */
    }
    #price::placeholder {
        font-size: 13px;
        color: rgb(140, 140, 140);
    }
    #price::-webkit-inner-spin-button,
    #price::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }
`;

// Вставляем элемент <style> в <head> документа
document.head.appendChild(style);
	},

	async E_app_Event140_Act1(runtime, localVars)
	{
// Создаем новый элемент <style>
const style = document.createElement('style');
style.type = 'text/css';

// Добавляем стили в элемент <style>
style.innerHTML = `
    /* Стили для элемента с id number */
    #number {
        background-color: transparent;
        border: none;
        outline: none;
        text-align: center;
        padding: 10px;
        -moz-appearance: textfield;
        -webkit-appearance: none;
        font-family: 'SFProText-Medium', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        font-size: 18px;
        color: rgb(125, 125, 125);
    }
    #number::placeholder {
        font-size: 14px;
        color: rgb(140, 140, 140);
    }
    #number::-webkit-inner-spin-button,
    #number::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }

    /* Стили для элемента с id price */
    #price {
        background-color: transparent;
        border: none;
        outline: none;
        text-align: center;
        padding: 10px;
        -moz-appearance: textfield;
        -webkit-appearance: none;
        font-family: 'SFProText-Medium', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        font-size: 16px;
        color: rgb(125, 125, 125);
        /* Убираем размытие */
        filter: none;
    }
    #price::placeholder {
        font-size: 13px;
        color: rgb(140, 140, 140);
    }
    #price::-webkit-inner-spin-button,
    #price::-webkit-outer-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }
`;

// Вставляем элемент <style> в <head> документа
document.head.appendChild(style);
	}

};

self.C3.ScriptsInEvents = scriptsInEvents;

